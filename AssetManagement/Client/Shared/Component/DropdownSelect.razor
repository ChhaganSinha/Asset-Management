@typeparam TValue
@inherits Microsoft.AspNetCore.Components.Forms.InputSelect<TValue>

<select id="@Id" class="select2 @Css" aria-label="..." disabled="@Disabled">


    @if (EmptyText == "true")
    {
        <option value="@string.Empty">-NOT SET-</option>
    }
    else
    {
        @if (EmptyText == "false")
        {
            //Ignore
        }
        else
        {
            <option value="@string.Empty">-@EmptyText-</option>
        }

    }

    @if (DataSource != null)
    {
        foreach (var item in DataSource)
        {
            if (item.Key == Value?.ToString())
            {
                <option value="@item.Key" selected="selected">
                    @((MarkupString)item.Value)
                </option>

            }
            else
            {
                <option value="@item.Key">
                    @((MarkupString)item.Value)
                </option>
            }

        }
    }

</select>